using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CookAreaScript : MonoBehaviour
{
    public GameObject connectedOven;

    // Start is called before the first frame update
    void Start()
    {
        /* To Do:
         * Make sure that, if the player is in this area, it calls the bakeCookies() function from the connectedOven gameobject;
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
